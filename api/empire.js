// api/empire.js
const axios = require('axios');
const cheerio = require('cheerio');

export default async function handler(req, res) {
  // ... aquí va tu lógica de scraping con axios y cheerio ...
  // Esto NUNCA llegará al navegador, solo el resultado JSON final.
}